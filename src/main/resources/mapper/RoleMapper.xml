<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hxpmc.pc.manager.dao.roleDao" >

    <select id="findRole" resultType="hxpmc.pc.manager.pojo.Role" parameterType="integer">
        select id,name,permission,role from role where id =#{id};
    </select>

</mapper>